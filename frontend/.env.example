# Local backend URL. Production should point to the deployed FastAPI /api path.
VITE_API_URL=http://127.0.0.1:8000/api

# Optional request timeout in milliseconds.
VITE_API_TIMEOUT_MS=20000

# Recommendation generation evaluates a large recipe pool and may take longer.
VITE_RECOMMENDATION_TIMEOUT_MS=90000
